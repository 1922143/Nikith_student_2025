let question = "Will I be happy?"; // Example question
console.log(question); // Output the question

const responses = [
    "Yes.",
    "No.",
    "Maybe."
];

// Get and output a random response
console.log(responses[Math.floor(Math.random() * responses.length)]);